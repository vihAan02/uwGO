import { Onboarding } from "@/components/onboarding/Onboarding";

/** First run after sign-in: paste the schedule, pick a home. `/plan` sends anyone without a schedule here. */
export default function SetupPage() {
  return <Onboarding />;
}
